// Shared tool settings model used across the editor (store, sidebar, tools).
export interface ToolSettings {
  color: string;
  strokeWidth: number;
  fontSize: number;
  fontFamily: string;
  textBgOpacity: number;
  arrowHeadSize: number;
}
